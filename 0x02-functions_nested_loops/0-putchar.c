#include <stdio.h>

/**
 * main - a program that prints _putchar, followed by a new line.
 * Return: Always 0 (Success)
 */
int main(void)
{
	char n[9] = "_putchar";

	printf("%s\n", n);
	return (0);
}
