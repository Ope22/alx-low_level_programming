#include "main.h"

/**
 * add - a function that add two integers and return the result.
 * @a: first parameter
 * @b: second parameter
 * Return: always 0
 */

int add(int a, int b)
{
	int c = a + b;

	return (c);
}
